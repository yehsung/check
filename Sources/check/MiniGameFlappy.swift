import SwiftUI

// [스텁 — feat/minigame-flappy 포크가 이 파일을 통째로 교체한다]
// 플래피 아잉: 규칙(FlappyGame, 순수 값 타입) + 잎 뷰(FlappyGameView).

struct FlappyGameView: View {
    let host: MiniGameHost
    let input: MiniGameInput

    var body: some View { Color.clear }
}
