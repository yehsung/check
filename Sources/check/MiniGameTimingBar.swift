import SwiftUI

// [스텁 — feat/minigame-timing 포크가 이 파일을 통째로 교체한다]
// 타이밍 바 게임: 규칙(TimingBarGame, 순수 값 타입) + 잎 뷰(TimingBarGameView).

struct TimingBarGameView: View {
    let host: MiniGameHost
    let input: MiniGameInput

    var body: some View { Color.clear }
}
